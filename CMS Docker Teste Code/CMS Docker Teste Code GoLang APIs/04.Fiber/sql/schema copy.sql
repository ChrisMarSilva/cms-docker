
--PostgreSQL:CREATE TABLE authors (id BIGSERIAL PRIMARY KEY, name text NOT NULL, bio text);
--MySQL: CREATE TABLE authors (id BIGINT  NOT NULL AUTO_INCREMENT PRIMARY KEY, name text NOT NULL, bio text);
--SQLite : CREATE TABLE authors (id INTEGER PRIMARY KEY, name text NOT NULL, bio  text);

